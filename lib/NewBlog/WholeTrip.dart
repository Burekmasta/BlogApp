import 'package:flutter/material.dart';

class WholeTrip extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Text('Whole Trip'),
    );
  }
}
